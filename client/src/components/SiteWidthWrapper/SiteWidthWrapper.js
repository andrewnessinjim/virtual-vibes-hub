import { css } from "styled-components";

const SiteWidthWrapper = css`
  max-width: 1200px;
  padding-left: 16px;
  padding-right: 16px;
  margin-left: auto;
  margin-right: auto;
`;

export default SiteWidthWrapper;
